package speek;

public enum TokenType {
    LET,
    BE,
    SAY,
    IF,
    IS,
    GREATER,
    THAN,
    THEN,
    REPEAT,
    TIMES,

    NUMBER,
    STRING,
    IDENTIFIER,

    PLUS,
    MINUS,
    STAR,
    SLASH,

    GREATER_THAN,
    LESS_THAN,
    EQUALS_EQUALS,

    NEWLINE,
    EOF
}